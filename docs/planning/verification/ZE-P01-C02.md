# ZE-P01-C02 verification

Execution date: 2026-09-21/22, Windows with Docker Desktop's Linux engine on WSL2.
Base: `293e6c2b2d0f011cc95f6d6cb192aa4d1c754737` (synchronized origin/master).
Selector result: READY, execution_authorized=true, LOCKED scope, C01 verified merged.
Branch: `feature/ze-p01-c02-persistence-durable-jobs`.
Requirements: NFR-007, OPS-004, OPS-005.

## Acceptance evidence

| Acceptance | Executed proof |
| --- | --- |
| Rollback leaves no outbox event | PostgreSQL tests prove implicit rollback, atomic success, audit-writer failure rollback, and fail-closed missing audit composition. |
| Concurrent workers claim one lease | Two concurrent PostgreSQL sessions hold their transactions open across a barrier; one claims the job. Separate concurrent outbox claims are disjoint. |
| Redis loss recovers durable jobs | Flush all wakeups from the isolated Redis database after dispatch; a database sweep still runs the intended job once. Redis-down dispatch also preserves intent. |
| Initial migrations run on PostgreSQL | Fresh isolated database upgrade, metadata drift check, repeat upgrade, previous application compatibility, and refusal of data-losing downgrade. |

Additional coverage: stale outbox/job/inbox tokens, unique event and job identities,
scoped claims, forged system-principal denial, absent worker HTTP route, immutable
payload triggers, restart after effect with stable deduplication identity, bounded
backoff/Retry-After, exact 24-hour expiry, eight crashed claims without a ninth
attempt, safe errors, verified-only inbox processing, domain state invariants and
aware clocks. Each database test migrates its own disposable PostgreSQL database.

## Executed checks

- `pytest tests/chunks/ZE-P01-C02 tests/chunks/ZE-P01-C01 -q`: **41 passed**
  (26 C02, 15 C01). Full output: `ZE-P01-C02-tests.log`.
- `uv run --locked ruff check apps/api/src tests/chunks/ZE-P01-C02`: PASS.
- `uv run --locked ruff format --check apps/api/src tests/chunks/ZE-P01-C02`: PASS.
- `uv run --locked mypy apps/api/src tests/chunks/ZE-P01-C02`: PASS, 27 source files.
- `uv run --locked python -m compileall -q apps/api/src tests/chunks/ZE-P01-C02`: PASS.
- `uv run --locked python scripts/validate_plan.py`: PASS, 219 requirements,
  62 chunks, 113 unchanged scope witnesses.
- `uv run --locked python scripts/next_chunk.py --validate`: PASS.
- `uv run --locked python scripts/reconcile_state.py --validate-plan`: PASS.
- `uv run --locked alembic upgrade head`: PASS against the explicit disposable
  PostgreSQL database; never an implicit/default or production database.
- `uv run --locked alembic check`: PASS, no metadata drift.
- `uv run --locked pytest tests/chunks/ZE-P01-C02 -m migration -q`: PASS,
  one migration test, 25 deselected non-migration tests.
- `uv build --no-sources`: PASS, wheel and sdist built with the pinned build backend.
- `git diff --check`: PASS.

- `python -m unittest discover -s tests/workflow -v`: **61 passed**. The full
  output is retained in `ZE-P01-C02-workflow.log`.
- `python -m py_compile tests/workflow/test_routing.py`: PASS.

Full static/build/migration output: `ZE-P01-C02-checks.log`. Web lint/type/Vitest/Next and Compose validation
are not applicable: no web/package or Compose files changed. CI expansion belongs
to the following chunk; the existing planning CI remains in place.

## Review and corrections

Independent critical/deep review: `memory/handoffs/ZE-P01-C02-review.md`.
The reviewer requested moving job lifecycle/retry policy from the SQL mapper to
the domain object, adding reconstitution invariants and testing claim exhaustion.
Those corrections are included in the 41-test run. No unresolved material finding.

Initial test issues were corrected without weakening acceptance: the migration
compatibility test now uses the existing `/api/v1/health` route; the workflow
forecast fixture explicitly sets its planned state instead of depending on C02's
live status. Existing exclusion/no-mutation assertions remain and in-progress
exclusion is additionally tested.

The Windows checkout's automatic CRLF conversion initially caused byte-based scope
witness validation to fail. Only unchanged witness files whose LF bytes exactly
matched the Git index were normalized in the checkout; no witness, approved plan,
Git setting or architecture content changed. Isolated Git workflow tests run with
process-only `GIT_CONFIG_COUNT=1`, `GIT_CONFIG_KEY_0=core.autocrlf`,
`GIT_CONFIG_VALUE_0=false` so their hashed fixture bytes survive fixture commits.
Two existing Starlette/httpx deprecation warnings remain non-failing.

## Scope and rollout

Three durable delivery tables, explicit UoW/Clock/version primitives, worker-only
dispatch/recovery, migration harness and relevant dependency/test wiring implement
this foundation. Settings/bindings business aggregates, concrete audit storage,
provider handlers and role-facing capabilities remain in their scheduled owning
chunks. The injected audit writer is fail-closed, not an in-memory persistence stub.

The migration expands schema without changing existing API startup. App rollback
keeps the new tables; destructive downgrade is refused. Production backup/restore
and rollout gates remain required in their owning operations chunks. Read
`apps/api/src/zuno_edu/infrastructure/persistence/README.md` for test setup and the
data-preserving forward-fix procedure. PostgreSQL 16 and Redis 7 Alpine images were
used only in isolated localhost test containers, which were removed with their
disposable volumes after database verification passed.

Context was loaded through targeted requirement, object, port, service, operation,
schema and narrative entries rather than whole catalogs. Shared primitives,
trusted composition and package wiring outside the
initial persistence path are necessary to preserve the documented dependency
direction. The workflow fixture was read for its concrete failing regression.
