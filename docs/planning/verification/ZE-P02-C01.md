# ZE-P02-C01 verification

Date: 2026-09-25. Base: PR #11 merge on master.
Branch: feature/ze-p02-c01-authentication-resume.

## Executed checks

- uv sync --locked --dev and pnpm install --frozen-lockfile: PASS.
- ruff check and ruff format --check over apps/api/src, tests/chunks/ZE-P02-C01 and
  tests/chunks/ZE-P01-C02/test_migrations.py: PASS (55 formatted files).
- mypy over the same targets: PASS (53 source files).
- pytest tests/chunks/ZE-P02-C01: PASS, 55 tests, 27.79 seconds.
- pytest tests/chunks/ZE-P02-C01 tests/chunks/ZE-P01-C01 tests/chunks/ZE-P01-C02
  tests/chunks/ZE-P01-C03: PASS, 125 tests, 41.72 seconds.
  Ran through uv run --locked with a fresh task-local --basetemp and
  -p no:cacheprovider to avoid Windows temporary-directory ACL conflicts.
- validate_plan.py: PASS, 219 requirements, 100% planned coverage, 114 scope witnesses.
- next_chunk.py --validate: PASS, 62 routed chunks.
- git diff --check: PASS.
- unittest discover -s tests/workflow -q: PASS, 61 tests, 297.126 seconds.
  Temporary repositories use process-local core.autocrlf=false so byte witnesses match.
- uv run --locked python scripts/check_secrets.py: PASS. Six exact synthetic test
  values are hash-allowlisted; production scanning rules remain unchanged.

PostgreSQL 16.15 and Redis 7 ran in disposable localhost-only containers. Each database
test created a unique disposable database. Alembic upgrade, metadata drift and
forward-only downgrade checks passed; the previous health application remains compatible.

Two pre-existing pinned Starlette/httpx deprecation warnings remain. Earlier missing-tool,
review-capacity and database blockers are resolved. No dependency version changed.

## Acceptance evidence

| Acceptance | Evidence |
|---|---|
| Wrong credentials, status denial and uniform recovery | test_authentication_service.py, test_http.py |
| Single-use reset, TOTP and recovery replay | service and database-service tests |
| Suspension and session revocation | service tests; current account read under Account lock |
| CSRF, Origin, secure cookies, expiry and idle refresh | HTTP and domain tests |
| Rate limits and outage denial | real Redis test_security.py; HTTP 429/retry tests |
| Limited login/invitation setup, no full session | service and HTTP tests |
| ADR 0004 first response/replay/conflict/restart | service tests; concurrent PostgreSQL duplicate enrolment |
| Rollback/uncertain commit | transactional fake tests; PostgreSQL audit failure rollback |
| Expired/consumed/foreign-browser/exhausted setup | service and direct repository tests |
| Preserve active factor until verified replacement | service replacement regression |
| Locking and persistence | reset/login serialization; MFA invalidation; observed lock waits crossing expiry; foreign-factor denial |
| Durable protected recovery and audit | encrypted token payload, UUID-only outbox, shared transaction, absent writer fails closed |
| Email-less student login | provisioned student Account UUID only; adult UUID denied |

## Password benchmark

Five hashes on the development host: median 201 ms, Argon2id 64 MiB, three iterations,
one lane, random 16-byte salt, 32-byte output. Local target: 100–500 ms with at least
64 MiB memory. Deployment must repeat the benchmark and supply breached-password
screening, managed keys, current staff policy and audited persistence.

## Context expansions

Targeted database-schema entries and existing UOW boundaries were read for the review's
concurrency/missing-dependency findings. Named schemas/ports used next_chunk.py's context
reader. Execution metadata stays synchronized while protected manifest body bytes remain
unchanged. UUID student-username mapping and composition requirements are documented in
apps/api/src/zuno_edu/modules/identity/README.md.
